import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  display: "swap",
  variable: "--font-inter",
});

export const metadata: Metadata = {
  title: "Build in the open - Build Your Company Openly",
  description: "Build in the open helps modern founders and entrepreneurs build their companies openly with tools, resources, and a supportive community.",
  keywords: ["founders", "transparency", "entrepreneurship", "open building", "community"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${inter.variable} font-sans antialiased`}>
        {children}
      </body>
    </html>
  );
}
